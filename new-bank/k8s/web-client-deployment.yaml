apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-client
  namespace: internet-bank
  labels:
    app: web-client
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-client
  template:
    metadata:
      namespace: internet-bank
      labels:
        app: web-client
    spec:
      containers:
        - name: web-client
          image: 395734437227.dkr.ecr.eu-central-1.amazonaws.com/internet-bank/web-client:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
